#include <stdio.h>
#include <string.h>

char a[1000009], b[1000009], c[1000009], d[1000009];

int main()
{
    int t, i, k, sum1, sum2;
    scanf("%d", &t);
    for(i=0 ; i<t; i++) {
        scanf("%s %s %s %s", a, b, c, d);
        sum1= ( (a[0]-48)*2)/10+( (a[0]-48)*2)%10+( (a[2]-48)*2)/10+( (a[2]-48)*2)%10+( (b[0]-48)*2)/10+( (b[0]-48)*2)%10+( (b[2]-48)*2)/10+
        ( (b[2]-48)*2)%10+( (c[0]-48)*2)/10+( (c[0]-48)*2)%10+( (c[2]-48)*2)/10+( (c[2]-48)*2)%10+( (d[0]-48)*2)/10+( (d[0]-48)*2)%10+
        ( (d[2]-48)*2)/10+( (d[2]-48)*2)%10;

        sum2=( a[1]+a[3]+b[1]+b[3]+c[1]+c[3]+d[1]+d[3] ) - (8*48);

        if( (sum1+sum2) %10==0)
            printf("Valid\n");
        else
            printf("Invalid\n");
    }
    return 0;
}
